# Copier vers `.env` à la racine du projet (même dossier que `manage.py`).
# Django charge ce fichier au démarrage si `python-dotenv` est installé (`pip install -r requirements.txt`).

# Django
# DJANGO_SECRET_KEY=
# DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
