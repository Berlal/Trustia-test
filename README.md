# Test technique — Trustia

Dépôt **public** : exercices 1 et 2 demandés.

| Dossier | Contenu |
|---------|---------|
| **`Ex 1/`** | Programme Python (un seul fichier : `exercice1.py`). |
| **`Ex 2/`** | Projet **Django** : produits (CRUD), facturation (lignes + quantités), **pagination** des listes. Voir **`Ex 2/README.md`** pour l’installation. |

**Lancement rapide (Ex 2)** : `cd Ex 2` → `pip install -r requirements.txt` → `python manage.py migrate` → `python manage.py runserver`.

Ne pas versionner : `.env`, `db.sqlite3`, `.venv/` (voir `Ex 2/.gitignore`).